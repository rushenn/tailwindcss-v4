# Changelog

## v1.0.0 — May 2026

### Added
- Rule 1: Enforce Tailwind CSS v4.2.x, block v3 patterns
- Rule 2: No unnecessary arbitrary values — opacity modifiers, spacing, aspect ratio, fonts, colors, CSS variables, shorthand collapsing
- Rule 3: Canonical class preference checklist (9 checks)
- Rule 4: Dark mode — media query default, `@custom-variant` class/data strategies, semantic token flipping, `next-themes` for Next.js
- Rule 5: Custom utilities with `@utility`, `@apply` rules, `@reference` fix for scoped styles
- Rule 6: `cn()` pattern with `clsx` + `tailwind-merge`, component `className` prop convention
- Rule 7: Variant stacking order rule (`responsive:dark:state:utility`)
- Rule 8: Next.js-specific setup — PostCSS, `tw-animate-css`, `@source` directive, complete `globals.css` template
- Rule 9: Prettier plugin with required `tailwindStylesheet` entrypoint for v4
- Quick anti-pattern reference table (20+ entries)
- Doc lookup guidance with key URLs
